# Project status

## 上传时状态：第二阶段真实视觉续训已启动（2026-09-15 23:27 HKT）

首轮搜索三种规则全部选择h3，尚无VETO独立选择收益。实际2048题与保存的
sampler状态预检已完成，控制器自动启动第二阶段原生续训作业233990，当前RUNNING，
使用2张H800。下一步是完整参数更新、图像消费、服务导出及开发评测核验；尚无第二阶段
最终成绩。GitHub代码与进展快照在独立目录整理，不修改正在运行的实验源文件。
下列旧条目均为各自时间点的历史状态，不能当作当前作业状态。

## 首轮搜索完成：三种规则同选h3，续训预检进行中（2026-09-15 23:14 HKT）

完整档案现已核验：h0的H/C配对为107/128、49/64；h1为111/128、54/64；
h2为111/128、48/64；h3为112/128、53/64。WHALE、VETO和单图准确率门
全部选择h3，恢复路径也一致。这轮没有提供VETO独立选择价值的证据。
`data/fast-chart-20260915-v1/first-search-report-v1/result.json`重算了全部1024条H/C
回答、格式分类及接受记录；四个H/C作业共0.688056GPUh，不含前缀/提案/续训成本。

控制器2574402正在执行预先指定的VETO四批续训预检，子进程2651574确认存活。
来源为真实LR1e-6 seed42的global_step_4和data.pt；正在核对2048题的共同取样集合
与下一四批顺序。尚未提交新的GPU训练，不是执行停止，不能跳过这项检查。

可编译稿`data/fast-chart-20260915-v1/paper-build-v15/`已纳入完整候选表和真实失败
案例；六页均已渲染检查。案例显示h2在文字中读出变化后的数值，却把减法顺序反了。
因此配对失效本身不证明“没看图”；正文已明确指标不能定位潜在机制。案例从C中事后
选取，保留全部六条真实回答、空工具轨迹和未改动的两张输入图，不作为频率估计。
证据：`results/fast-chart-paper-v15-visual-review-20260915-v1.json`；
`data/fast-chart-20260915-v1/illustrative-case-v1/case.json`。正文主要方法结果仍为空，
案例图浮到独立页等排版问题也保留待修改，当前稿不是可投稿候选稿。

最新已结14.266111/100GPUh，无GPU在途预留；新增占用93.37/400GiB。
清理与后续阶段额度调整仍待回复；首轮已授权续训可以继续。三个种子的科学门槛尚未
完成评估，不能宣称通过；seed42本轮没有选择差异。

### 22:58时点及更早记录

首个候选h1作业233589已完成，计0.191944GPUh：H111/128，C54/64对，单图115/128。
相对h0两项都提高，配对门与单图门均允许h1，不能据此认定VETO有独立价值。
原始结果重算见`results/fast-chart-first-search-h1-count-review-20260915-v1.json`。
已有3个真实GLM候选，未补充或替换候选。有限交接进程2574402已认证并附加h1，
第二候选h2作业233705已完成，计0.181389GPUh：H111/128，C48/64对，单图107/128。
相对incoming普通准确率+3.125个百分点，配对-1.5625个百分点；配对门和单图门都会拒绝。
这是优化集上的真实权衡，不能当作独立方法收益或旧2个百分点投入门槛已通过。
证据：`results/fast-chart-first-search-h2-tradeoff-review-20260915-v1.json`。
第三候选h3作业233759已于22:57:43启动，1张H800、setup预留1小时，仍在运行。
控制器顺序完成h2/h3、原生接受规则比较、预先指定的
VETO四批续训与完整参数/图像/V512核验。只使用首轮setup额度，不放行18条件矩阵。
4项交接检查通过，覆盖重复提交、模糊提交、结果可见性延迟与失败候选保留。
代码对应E1→E2–E3→E4；未改变选择规则、训练损失、评分或候选内容。
启动记录：`results/fast-chart-first-cycle-controller-launch-20260915-v1.json`。

新增完整候选档案报告入口`ours/fast_chart_search_report.py`：重算E1并重放普通/恢复
两条E2–E3选择路径，保留失败槽位、相同选择及H/C成本边界。论文生成器可通过
`--search-report`引入其表格和证据索引；缺失候选或证据变化会拒绝生成。新增共同解析
诊断后5项CPU检查通过，但完整真实档案尚待h3，尚未生成这张实测完整表，也未声称
通过方法验收。h0/h1所有512条H/C记录已独立复核解析与正确性：H无法解析均为4条；
C从4条降为1条，也存在可解析错误变为正确的记录。该描述不能归因于视觉能力提升。
证据：`results/fast-chart-first-search-format-diagnosis-h0-h1-20260915-v1.json`。

incoming h0的233540已于22:09:17完成：H107/128，C49/64对，两侧单图110/128。
这些是优化数据，不是独立性能结果。GLM本轮6次请求、保守计费0.028800元；项目累计
保守已结1.029392元，另有历史未决预留6.38976元，不能标为服务商账单。
原发送授权在本线程2026-09-10“我授权发送”中可核查；自动审核初次拒绝后，补充原授权
及当前H专属内容清单，重新审查通过。未绕过审核，没有再次要求用户批准相同范围。

必须报告的限制：实际提案轨迹只读取h0代码，并尝试不存在的迭代报告，未读取已提供的
H反馈。因此3个候选属于通用策略变体，不能称为依据H反馈生成的改进。原始候选继续
接受相同评测，不重新付费生成。证据：`results/fast-chart-first-proposal-tool-use-review-20260915-v1.json`。
CLI的latest日志软链接已可逆转换为同内容硬链接，原日志完整保留；未删模型/检查点，
未放宽存储扫描规则。`results/fast-chart-cli-log-alias-normalization-20260915-v1.json`。

当前已结14.150833/100GPUh，h3在途预留1小时；新目录实测93.05GiB（上限400）。
首轮最高新增6GPUh已在查看候选结果之前登记，可在原setup20小时内完成。清理与后续阶段额度调整仍待
明确回复，两者不阻止本次有限闭环。尚无VETO方法优越性或封存测试结论。

### 22:00时点记录（已被上述状态更新）


当前GPU作业233540于21:57:19在1张H800启动，评测选中LR=1e-6的更新后模型，
覆盖H128题与C64对。属于前3天20GPUh准备/闭环额度；不等待清理，不是全矩阵放行。
自动交接进程2431747负责完整结果认证，下一步为真实候选生成、接受和续训。

吞吐作业233485已于21:56:29 COMPLETED，耗时30分29秒，计0.508056GPUh。
完整8/16/32并发分别需939.78/729.22/528.15秒推理512图；按预定速度规则选32，
共用T/R/ChartQA配置已冻结。对应单图452/448/447，配对214/213/210；所有1536条
原始回答逐项复核一致。调度会改变答案，未按最高准确率挑并发。
证据：`results/fast-chart-throughput-completion-review-20260915-v1.json`。

完整预测约80.66/100GPUh（含剩余重跑预留）；最终评测23.52小时超过原阶段额度20。
建议核心对照40→35、最终评测20→25，总计100，等待明确确认；代码额度尚未改。
清理授权也仍待确认：全部保留约700.51GiB；按具体清单回收后暂估峰值373.63GiB。
两项待确认都不阻碍当前有限闭环。详情：
`ours/fast_chart_phase_reallocation_20260915.md`、`ours/fast_chart_retention_20260915.md`。
当前累计已结13.578056GPUh，在途setup预留1小时；尚无VETO方法收益或封存测试结论。

### 先前阶段与核验记录

活动goal已重新建立，继续执行已授权的精简计划。作业失败后只报告而未接着修复，
属于执行中断；不是需要用户再次授权，也不代表研究目标已经完成。

已实现同步前闲置显存释放：在原生NCCL申请CuPy缓冲之前释放PyTorch/CuPy闲置缓存，
核对活跃分配字节数不变。此处属于E4共享权重传输，不改变VETO接受门、损失、模型
精度、3GiB传输桶、学习率、样本顺序或256条轨迹预算。9项修复/恢复相关检查通过。
真实GPU首批已完成：56条成功轨迹、7次原生SFT更新、7370个loss token；同步前释放
24.395GiB闲置显存，活跃分配字节数不变，原生权重同步成功返回。旧OOM位置已通过，
四批训练与最终检查点已完成；全参数核验也通过。同步修复证据：
`results/fast-chart-post-sft-transport-check-20260915-v1.json`。

吞吐测量作业233485于21:26:00启动，1张H800、最多1小时；运行两次完整V512，
分别batch16和32，与既有batch8比较，按秒/图选共同独立评测调度，保留所有答案变化。
计划：`results/fast-chart-throughput-plan-20260915-v2.json`；旧v1未提交并保留。
新增数据准备分段计时，将T/R的2048图准备成本纳入预测，不改变图像、模型或评分。
11项检查通过，属于E1共享执行与成本核算。完整18条件资源预测仍待实际吞吐结果；
没有启动正式矩阵、删除检查点、调用新增付费proposer或读取封存测试。

首个seed42 H128+C64对测量属于已授权前3天20GPUh“修正链闭环”额度，
计划预检已通过；233485与记账结束后，在setup余额内单独提交。当前空间可容纳，
无需先清理模型。此步不等于放行整个矩阵；共用时只记一次成本并扣除后续预测工作量。
记录：`results/fast-chart-seed42-setup-hc-admission-20260915-v1.json`。

吞吐第一遍batch16已完整完成：512图推理729.22秒，1.424秒/图，
相对既有batch8的1.835秒/图更快；输入准备实测152.455秒，已包含在加载时间。
单图448/512、配对213/256，调度改变的答案也保留。batch32仍在运行，尚未选并发数。
有边界的交接进程2431747已启动，4项检查通过：等待完整吞吐终态与文件可见，
冻结共同策略、记录全套资源预测，然后只提交一次setup H/C测量；不自动重试、
不付费提案、不训练、不清理、不放行矩阵。当前状态`WAITING_THROUGHPUT_ALLOCATION`。
启动记录：`results/fast-chart-setup-hc-controller-launch-20260915-v1.json`。

新控制器：`data/fast-chart-20260915-v1/lr-calibration-v3/`，当前为
`COMPLETE_SHARED_CHART_CONFIGURATION`，已完成全部三个学习率，按普通V准确率选定1e-6。第三项训练233361已COMPLETED，
20:31:05–21:01:41，两张H800共1.02GPUh。四批256条轨迹中144条成功
（52/35/42/15），完成20次实际LR=1e-5的原生更新，四次更新后的同步均返回。
证据：`results/fast-chart-third-lr-training-completion-20260915-v1.json`。
后续233399于21:04:43在一张H800上启动，21:22:00完成，计0.288056GPUh。全部144条实际图像输入、
144次视觉前向和非零有限梯度已逐项核对通过；完整参数比较也已通过：原生FP32有
4,538,504,425个元素变化，服务BF16有2,629,039,107个元素变化，导出逐项等于规定转换。
V512绑定已核验服务权重：单图280/512（54.69%）、配对89/256（34.77%）。
相对共同初始下降33.594和48.828个百分点，完整保留，不作为VETO效果。
证据：`results/fast-chart-third-lr-parameter-handoff-20260915-v1.json`。
三项训练计划核对：输入、四批采样顺序、h0、模型、源码与配置均相同，仅LR和输出路径不同。
证据：`results/fast-chart-third-lr-start-20260915-v1.json`。作业233172已COMPLETED，
19:17:48–19:55:32，两张H800共1.257778GPUh，属于setup额度。
四批256条轨迹中192条成功（55/47/44/46），完成25次实际LR=1e-6的参数更新，
四次更新后的同步均返回。全部192条图像消费与梯度核验通过；完整FP32参数及其BF16
导出均有真实变化，服务权重与原生转换逐项一致。后续233258已COMPLETED，
19:58:25–20:18:20，计0.331944GPUh。完整V512为448/512（87.50%）单图正确、
207/256（80.86%）配对正确；比共同初始分别下降0.781和2.734个百分点。
逐题固定验证器重算一致：13对改善、20对变差；这是校准结果，不是VETO效果。
证据：`results/fast-chart-lr-1e-06-development-review-20260915-v1.json`。
证据：`results/fast-chart-second-lr-parameter-handoff-20260915-v1.json`。
与1e-7试验核对：完整样本、四批顺序、模型、h0、源码和训练配置均相同，仅LR及输出路径不同。
证据：`results/fast-chart-second-lr-start-20260915-v1.json`。
作业232741已COMPLETED，18:02:20–18:40:26，2H800共1.27GPUh。
四批256条轨迹中191条成功，完成25次原生更新，保存global_step_4；四次更新后的同步均返回。
逐图核验191条成功轨迹与191次实际视觉前向、191次非零有限梯度一一对应。
后续作业233025已COMPLETED，18:43:34–19:08:39，计0.418056GPUh；完整V512评测通过。
LR=1e-7的单图443/512（86.52%）、配对212/256（82.81%），低于共同初始h0的
452/512（88.28%）、214/256（83.59%）。这是完整开发校准结果，保留下降，不是VETO效果。
三个学习率全部完成并逐题重算一致；按原规则选1e-6，其seed42第一阶段检查点作为共同前缀。
完整复核：`results/fast-chart-complete-calibration-review-20260915-v1.json`。
原生FP32共3,987,868,492个元素变化；实际服务BF16仍有95,166,505个元素变化，
其中视觉部分3,581,729个。导出等于原生参数的完整BF16转换。变化本身不等于性能收益。
证据位于`lr-calibration-v3/lr-1e-07-seed42/followup/`的
`image-consumption-audit.json`、`transition.json`与`result.json`。
逐题复核见`results/fast-chart-lr-1e-07-development-review-20260915-v1.json`。
首个1e-7试验从共同初始模型重跑，计入原定5GPUh重跑额度；
随后继续其余学习率校准。新作业使用独立计划名和目录，旧失败证据
全部保留；失败状态也会同步写入controller-status，避免继续显示WAITING。
修复记录：`results/fast-chart-native-transport-recovery-20260915-v2.json`。
v2的CPU预检未继承训练脚本的线程限制，实际启动72线程；在未提交GPU作业时已停止，
完整保留其日志和中断记录。v3采用与训练脚本相同的单线程设置，重新执行完整预检，
不会跳过实际sampler身份和顺序核对；该CPU中断没有产生GPU费用。

第二项评测结束时，共享磁盘的完成记录稍晚于Slurm终态可见，控制器因此遇到
FileNotFoundError。原始完整结果随后出现，全部计划/参数/评测哈希核验通过，没有重建结果。
已增加最多300秒的结果可见性等待和显式恢复入口，5项检查通过；原始失败记录和源码保留。
恢复进程2231441已正常完成同一v3目录，前两项没有重跑。此修复只影响E4完成交接，不改变实验。
记录：`results/fast-chart-controller-visibility-recovery-20260915-v1.json`。

截至此次核查累计已结13.07/100GPUh，在途setup预留1GPUh，未预留余额85.93GPUh。
其中重跑已用1.27/5GPUh。21:06核查新增实际占用90.17GiB，文件系统剩余571.22GiB。
没有新增API调用或清理文件。新增占用仍远低于400GiB；每次提交继续核查存储上限。
后台作业链若失败会保留证据；活动goal继续诊断，不把脚本停止当作整个目标完成。

共享评测吞吐计划已完成CPU预检：同一V512，实测batch16/32并对照现有batch8，
仅按速度选择，保留所有答案变化。2项约束检查通过，尚未提交GPU；等待当前校准链
结束后再运行，不与单作业额度冲突。此项属于E1执行调度，未改变接受规则或训练预算。
统一endpoint设置与批量32完整512题的CPU模拟检查共7项通过。只有真实吞吐测量和
Slurm终态完成后才能固定设置；所有方法的T/R/ChartQA统一使用，独立评测后禁止改选。
成本预测已接入测量入口并计入轨迹/评测证据空间，尚不计入未经测量的速度收益。
成本总账与显式共享分摊入口已接通，2项检查通过。当前API项目账本为已结算估算
1.000592元、未结算预留6.389760元；这是项目保守核算，不是服务商账单或余额。
方法分摊仍待真实分支完成后固定，不把共享前缀重复称为独立实验，也不省略失败费用。
9月15日官方国内费率已复核：输入0.8元、输出2.8元/百万token；项目继续用更高的
1/4费率保守记账，保留所有旧结算和未结算预留，无新增付费请求。来源与哈希见
`results/fast-chart-glm-public-pricing-20260915-v1.json`。

论文paper-build-v12编译通过，补充E1解释边界、官方来源引用及共同精度设置；正式结果仍待实验。
完整成本预测将计入所有已完成LR试验中较慢的模型加载时间，相关3项检查通过。
必须在三项LR和共享吞吐测量结束后运行新的完整预测。旧CPU watcher已因控制器的历史
可见性故障记录停止；其旧预览不作准入依据，由活动goal在完整校准后继续实测和预测。

## 暂停历史记录（2026-09-15 17:02 HKT核查，已由上节恢复）

训练作业232265于16:14:58以FAILED结束，运行17分42秒，计费0.59GPUh。
失败发生在第一批训练后的权重同步：CuPy申请3GiB传输缓冲时显存不足。
控制器已记录`STOPPED_REQUIRES_DIAGNOSIS`，后续学习率试验没有继续提交；
CPU复核记录为`CALIBRATION_FAILED_NO_EXPANSION`。这不是磁盘空间不足或模型成绩差。

累计已结算8.484167/100GPUh，在途预留0，剩余91.515833GPUh。
共同提示词校准已完成并保留；四batch训练、学习率选择、真实选择后续训仍未完成。
下一步需修正权重同步的显存占用，在保留失败证据和同等训练预算的前提下重跑。
当前没有完成修复或重新提交。

证据：`data/fast-chart-20260915-v1/allocations/lr-1e-07-seed42-training-plan/slurm-terminal.txt`、
`data/fast-chart-20260915-v1/lr-calibration-v1/controller-failure.json`及
`data/fast-chart-20260915-v1/lr-calibration-v1/lr-1e-07-seed42/training/failure.json`。
`controller-status.json`保留了失败前的WAITING状态，应以失败记录和终态为准。

## 精简图表计划及失败前进度（2026-09-15）

主线为一个4B模型、一次完整交替、三个种子的18个逻辑条件。共同前缀共享；
51组矩阵、Chess、CLEVR、2B继续暂停。总预算100GPUh包含历史视觉6.898056GPUh，
新增存储上限400GiB，项目API累计上限45元。没有删除既有文件或推送GitHub。

**共同初始提示词校准已完成，作业231666正常结束。** 三种提示均评测完整256对V：

| 提示方式 | 单图正确 | 两图同时正确 | 用途 |
|---|---:|---:|---|
| direct |427/512，83.40%|202/256，78.91%|共同起点校准|
| structured |452/512，88.28%|214/256，83.59%|共同起点校准|
| brief_reasoning |442/512，86.33%|208/256，81.25%|共同起点校准|

这些是新统一评分协议下的真实开发结果，**不是VETO收益或正式测试成绩**。
按普通开发准确率选择共同h0，平局按生成token；最终选定structured。

校准控制器已启动：`data/fast-chart-20260915-v1/lr-calibration-v1/`。
第一个学习率1e-7的预检通过，原生训练作业232265已启动（两张H800，四batch）。
首批64条轨迹中53条答对，已观察到非零且有限的视觉梯度；这是训练过程记录，
不是开发/测试分数，四batch完成及全参数更新核验仍待结束。
控制器依次运行三个固定学习率的4batch训练与完整V复评，选定共同
学习率后停止。每项先预留资源、按Slurm终态结算；失败停下，不自动追加候选或重跑。
控制器不会启动正式矩阵、调用GLM、访问T/R或删除检查点。实时状态见其
`controller-status.json`，不要重复启动。
CPU跟进进程会在校准结束后生成`lr-calibration-v1/expansion-forecast.json`；
若校准失败或证据不完整，报告原因并停止，不启动正式条件。

已完成：三类题均衡数据构建、原始分组和像素/数值校验；统一host解析和评分；
修正后真实sampler参考核验；ChartQA固定512题下载与精确图像/表格重叠筛查。
22项新集成/投入门/完整性检查通过，另有8项历史恢复检查通过。
真实2048题、四batch采样输入预检通过；正文、补充和方法图已编译。

新增入口已覆盖4batch原生训练/恢复、全参数导出核验、3候选搜索与三个接受规则、
50%配对增强、独立评测注册、成组区间和证据图表。**这些新的训练—搜索—续训连接
尚待真实GPU闭环验收，不能把CPU检查当作科学结果。** 18个条件结果尚未完成。

16:00HKT资源快照：累计已结7.894167GPUh（本次提示校准0.996111），在途预留2GPUh，
未预留余额90.105833GPUh；新增实验目录约2.33GiB，尚无新训练检查点。
正式扩展前仍要用实测吞吐和模型文件大小
核算总预算与400GiB存储；保留全部原生最终权重和服务副本可能超限，不会自动清理。

**实测评测吞吐初算有缺口**：按现有串行方式，18个测试终点、6个共同h0复测和18个
ChartQA子集评测预计33.84GPUh，超过最终评测阶段20GPUh额度。此为保留加载成本的
外推，尚非实际最终支出；候选输出长度和外部吞吐仍未知。校准继续；正式扩展前需
验证共同批量评测提速，并补齐训练/存储测算。没有删种子、降低某个基线配置或调整额度。
记录：`results/fast-chart-throughput-preview-20260915-v1.json`。

入口与说明：`ours/fast_chart_runbook.md`；冻结协议：`ours/fast_chart_protocol_20260915.md`；
本轮代码/验证/运行交接：`results/fast-chart-implementation-handoff-20260915-v1.json`；
矩阵：`results/fast-chart-matrix-20260915-v1.json`；当前稿件：
`data/fast-chart-20260915-v1/paper-build-v8/main.pdf`及`supplement.pdf`。
补充材料已纳入三种提示词的真实开发校准表；正式结果仍为空缺。
9月10日旧搜索不作为修正后权重、新评分或新正式条件的结果。以下均为历史记录。

## 2026-09-10历史结论：真实视觉训练与训练后评测已核实

**本次授权的GLM调用已完成，6次请求，估算0.084845元。** 旧theta1上的候选搜索和
接受已完成；核查又发现旧训练遗漏像素，因此已修复并从共同初始模型重跑。

现在确认：模型确实通过图像学习，视觉与语言参数都发生了实际更新。相同开发题、
相同提示词的成绩，训练前后都是**117/128张图答对、53/64对同时答对**；4张图的答案
改变，总分保持。逐样本输入一致性也通过。此次一个小批次没有显示总体退化或提升，
不能据此证明VETO有效，也不能推断视觉退化普遍不存在。

**下一项尚未完成的是：修正后权重上的策略选择，以及使用所选策略继续学习。**
旧theta1上的h1接受记录不能跨权重复用，旧训练的退化记录也不能充当正确视觉训练
产生退化的证据。三个独立种子的科学门槛尚未验证；51组矩阵、Chess续训继续暂停。

224347训练、224358参数/开发评测均完成，当前阶段累计6.898056GPUh，无在途GPU
预留。修复过程没有新增API调用或删除数据。所有旧结果和失败代码保留，修正模块
对应共享E4训练适配，不作为VETO创新。完整证据索引：
`results/visual-corrected-engineering-milestone-20260910-v1.json`。

以下为执行过程记录，以本节为当前结论。

## 最新进度：核验修正后模型（2026-09-10，224358）

224347的逐样本重放与真实视觉前向/梯度匹配已全部通过：46条成功轨迹、46次
非零视觉梯度、6次更新、92个训练token。纠正后训练链的实际回执可通过续训检查；
旧文本损失训练链被明确拒绝，7项恢复CPU测试通过。

224358已提交：一张H800、最多1小时，比较所有原生FP32参数、导出一个服务权重，
然后以同一h0评测原有64对V图。无新训练或API调用；没有访问测试集或启动51组。
当前已结6.766667GPUh，预留1GPUh。全参数比较已通过：723个原生FP32张量均有变化，
其中297个视觉张量全部变化；导出BF16后455个张量仍有数值变化（视觉163/297）。
导出逐参数等于原生权重的BF16转换，原恢复文件保持；开发分数及作业终态仍待完成。
证据：`results/visual-pixel-parameter-comparison-20260910-v1.json`。

## 修正训练已返回（2026-09-10，224347）

46条成功轨迹已完成46次真实视觉前向和46次非零视觉特征梯度；原生RSFT返回，
执行6次参数更新、92个assistant loss token，checkpoint已生成。逐样本批次/像素
重放审计与完整参数比较仍待完成，因此暂不把“有梯度”写成已证明全模型参数变化。
旧224041的像素缺失结论仍有效，不能混用两条训练链。
Slurm已完成：559秒、0.310556GPUh，阶段累计6.766667GPUh。64条轨迹的图像/输入/
答案重放与46条成功子集核验通过；实际视觉模块记录的逐样本匹配正在单独核验。

## 修正重跑的最新状态（2026-09-10）

224346在首次学习前因未处理原生`images_seqlens`字段失败，没有生成检查点；
189秒、0.105GPUh已结算，累计6.456111GPUh。已依据原生agent-loop定义增加
图像长度与grid一致性校验，实际像素继续传入模型。5项真实小型模型CPU测试通过，
v3预检已通过并提交作业224347（2卡、最多1小时）；旧失败代码快照、轨迹及费用
全部保留。修正后的GPU更新结果仍待核验。
协议补充：`results/visual-pixel-forward-correction-20260910-v2.json`。

## 正在验证修复（2026-09-10 21:27，香港时间）

修正首批作业224346已提交，2张H800、最多1小时。共同初始权重、h0、W32子集与
首批8题/64轨迹的CPU核对一致；只更换像素forward及观察接口。当前预留2GPUh，
已结6.351111GPUh。首次CPU预检因沙箱本地多进程通信受限失败，未运行GPU；
使用允许本地通信的预检后通过，旧失败目录保留。新计划：
`results/visual-pixel-rsft-plan-20260910-v2.json`；GPU结果仍待核验。

## 关键纠正（2026-09-10，视觉首批训练）

**已确认旧训练没有将像素送入损失计算；不能再把224041称为已完成的多模态RSFT更新。**
WHALE针对Qwen3.5走了通用文本fused接口，像素与grid参数被接收后丢弃。真实小型
Qwen3.5复现显示：旧接口换图输出不变、视觉模块未调用且无梯度。既有全参数比较中
297个视觉张量全部未变、426个语言张量全部变化，与此一致。以前保存图像批次的核验
不足以证明损失实际使用图像，这是本轮纠正的验证缺口。

`ours/qwen35_visual_fused.py`改为调用原生多模态backbone，并继续使用WHALE同一个
fused token likelihood及原RSFT损失、mask、成功过滤。4项CPU测试通过，包括与原生
HF逐token概率和梯度一致、真实换图响应、缺失像素拒绝、实际视觉前向/梯度记录。
这是共享E4适配修复，不是VETO创新。GPU视觉梯度及新权重仍待真实重跑核实。

**下一步从共同初始模型重跑同一h0、同一W批次；不续训旧theta1来冒充修正后的闭环。**
旧权重、数据、分数、接受档案全部保留，首次图像推理及h1推理成绩仍是真实测量，
但训练反馈机制的解释暂时撤回。修复仅做一个2卡/1小时上限的小作业，无新API或删除；
51组矩阵、Chess续训继续暂停。协议：`results/visual-pixel-forward-correction-20260910-v1.json`。

## 本次授权调用已完成（2026-09-10，224335）

真实GLM h1：6次请求，估算0.084845元。完整评测512张图完成，1.0375GPUh；
第一遍H128/128、C64/64对；重复H128/128、C63/64对，有1张图答案波动。
第一遍用于冻结选择；原生完美分数早停仍经过E2–E3门，WHALE与VETO都选h1，
恢复复核也选择h1。优化集结果不等于独立泛化，更不证明VETO接受门有额外贡献。
当前已结6.351111GPUh，无在途预留；正式扩展关闭。这里的接受记录绑定旧theta1，
不能直接用作修正训练后的候选接受证明。

证据：`data/visual-search-bridge-theta1-20260910-v1/accepted-for-training.json`、
`data/visual-search-bridge-theta1-20260910-v1/acceptance-resume.json`、
`results/visual-search-theta1-h1-allocation-20260910-v2/allocation-result.json`。

**自动推进状态：用户已明确授权此次视觉提案外发，继续小规模视觉闭环。**
2026-09-10收到“我授权发送”，已核对原清单8份输入、43,161字节及源码哈希未变。
本次仅生成一个真实h1，沿用45元累计API上限、最多12次请求、无自动付费重试。
授权记录：`results/visual-proposer-authorization-20260910-v1.json`。
下面等待授权的旧摘要保留为历史；51组矩阵、Chess续训与新增清理仍暂停。

## 最新研究判断（2026-09-10 21:00，香港时间）

**首遍h1：H搜索128/128、C配对64/64；h0分别115/128、47/64。** 新策略改善了这批
优化题，但没有出现预登记的“普通分数不降、配对退化”。完整重复测量仍在进行，
尚未生成最终接受回执或theta2，也没有独立测试或VETO优势结果。

新增错误分布核对发现：已完成的C记录均无“两边都错”的配对。此时配对正确率P与
同批两边单图平均正确率M满足P=2M−1；默认epsilon=0下，配对门与简单同C准确率门
对当前候选给出相同筛选。此结论只针对已观察记录，不声称两种指标普遍等价。V组
仅作描述，不用于接受。当前不能把h1提升归为VETO的独立作用，51组扩展保持关闭。

h1的C回复用旧h0解析规则重放，为122/128、59/64对，仍高于h0；原128/128、64/64
保持。解析恢复会改变分数幅度，不能当因果贡献比例。后续先完成本轮闭环及固定h0
复测，保留格式、训练量与推理波动等替代解释；不放宽科学门槛或改写普通准确率定义。

证据：`results/visual-search-h1-first-pass-components-20260910-v1.json`、
`results/visual-h1-C-parser-replay-20260910-v1.json`、
`results/observed-pair-error-decomposition-20260910-v1.json`、
`results/visual-current-scientific-gate-assessment-20260910-v1.json`。

## 最新局部结果（2026-09-10 20:35，香港时间）

**h1已完成H128搜索题，128/128；h0为115/128。C与重复测量仍未完成，尚未接受h1。**
逐题重算确认13题由错变对、115题保持正确。H参与了提案优化，不是独立测试。
新策略生成27,460 token，旧策略256 token；更长的读图解释带来明显额外开销。

同一批h1原始回答重放旧h0答案提取器，仅120/128；8条需要h1的格式恢复。此结果说明
分数依赖输出处理，不能把净增13题全部分配给某个视觉机制，也不能把8题直接当成因果
贡献。它是保存答案的事后解析诊断，不是重跑模型的消融。冻结主评分与候选代码保持。
后续固定h0的训练前后复测须同时排查格式漂移；原分数和共享参考解析诊断并列保留，
不能择优采用，也不能把格式错误诱发的下降当成视觉证据失效。

已有theta1/固定h0的首次V128回复按两个解析器复核，仍同为113/128、49/64对；
没有修改旧成绩或选择较高重复。此参考将用于后续排查格式漂移，实际theta2尚未生成。
证据：`results/visual-theta1-fixed-h0-parser-reference-20260910-v1.json`。

证据：`results/visual-search-h1-complete-H-component-audit-20260910-v1.json`、
`results/visual-h1-H-parser-replay-20260910-v1.json`。原生H完美分数可能触发搜索早停，
仍须完成E2–E3接受与恢复核验后才可进入E4。224335继续，51组矩阵与新增清理暂停。

## 续训输入检查（2026-09-10，h1评测期间）

真实GLM候选h1的CPU输入预览通过：仍是原32道W子集，恢复后下一批8题与已保存顺序
完全相同，图像输入与候选身份保持。没有据此接受h1，也未执行第二次训练或新参数更新。
当前224335仍按冻结512图协议测量；原观察进程3302545在宿主机上正常运行。沙箱中的
PID不可见不是进程终止，已用原工具会话和宿主进程核实，没有重复启动观察器或作业。
证据：`results/visual-h1-resume-input-preview-20260910-v1.json`。

## 最新执行（2026-09-10 20:13，香港时间）

**已按用户授权发送，并获得首个真实视觉候选h1；完整对比正在运行，尚无最终分数。**
本次6次GLM请求，本地估算0.084845元，项目45元累计上限保持。候选会输出较长的读图
解释，实测约7秒/题，整套512图测量预计约1小时。原224334的30分钟上限不足，已在
完成34道H后明确停止，未产生完整H/C或可用选择分数；464秒/0.128889GPUh全部计费。
依据吞吐而非得分，把同一候选的完整评测上限改为90分钟，224335已提交；模型、题目、
解码、首次完整遍选择及重复检查均不变，没有重新调用GLM或补改候选。

当前协议已结5.313611GPUh，新作业预留1.5GPUh，仍在第一阶段100GPUh内。51组矩阵、
Chess续训和新增清理继续暂停。本次只复用模型做小验证，不产生新模型检查点。
源代码和结果保留本地；按既有规则配置状态邮件，未验证邮件实际送达。

证据：`results/visual-search-h1-time-limit-revision-20260910-v1.json`、
`results/visual-search-theta1-h1-allocation-20260910-v1/allocation-result.json`、
`results/visual-search-theta1-h1-plan-20260910-v2.json`、
`results/visual-search-theta1-h1-allocation-20260910-v2/submission.json`。

完成后先核实所有H/C和作业终态，再执行真实E2–E3接受；接受完成后才准备E4续训。
新增续训批次核验器尚未用于真实第二批；FP32比较器2项CPU小张量测试通过，不能写成
实际GPU恢复或训练完成。新代码对应E4证据核验，不增加方法模块。

## 当前执行（2026-09-10 20:04，香港时间）

用户已授权外发，GLM完成一个真实视觉h1，6次请求，本地费用估算0.084845元，
项目已核算累计1.000592元，另有历史未结算预留6.38976元；都不冒充官方账单。
候选主要强化读数、图例和最终答案指令。原生搜索已推进至候选测量，224334已提交：
同一theta1、同一H/C与解码，第一遍决定选择，重复遍仅检查波动；最多0.5GPUh。
当前仍只做单域/单模型/单种子视觉闭环，没有启动51组矩阵或新增清理。

GLM的文字报告误称h0为82/128，真实逐题记录仍是115/128。原报告保留，错误已单独
记录，选取只读取实际测量；这不是更改基线、接受候选或性能提升的证据。
证据：`data/visual-search-bridge-theta1-20260910-v1/proposal-ready.json`、
`results/visual-proposer-narrative-audit-20260910-v1.json`、
`results/visual-search-theta1-h1-allocation-20260910-v1/submission.json`。

## 最新摘要（2026-09-10 17:58，香港时间）

**视觉小验证继续推进，51组正式矩阵与Chess续训仍暂停。** 224196已完成512张图的
H/C两遍测量：H单图115/128、117/128；C配对47/64、48/64。串行推理仍有少量波动，
按冻结计划只用第一遍参与选择，不挑较高分。H/C都是优化数据，这些数值不是独立
验证效果，也不能证明VETO有效。作业630秒、0.175GPUh已结算；本协议累计5.184722GPUh。

真实h0的H/C记录已接入原生搜索循环并完成初始接受。新增连接覆盖普通结束、完美分数
提前停止和恢复后接受；4项CPU检查通过，其他候选仍是测试夹具，尚无真实视觉h1。
本地实际sampler恢复检查也已通过：下一批8道W题保持图像输入、与上一批8题不重叠；
未执行GPU actor恢复或第二批训练。

等待外发授权期间，已补齐E4视觉续训入口：从完整搜索接受回执恢复原生actor、scheduler、
RNG及sampler，沿用未保存Adam moments的原约定；原权重传递完成后，在实际接收模型中
核对8个参数坐标，再允许生成新轨迹。6项CPU检查通过，包含真实原生loader的小张量
恢复、随机序列保持、旧接收权重拒绝、原同步先于接收检查，以及worker初始化不重复。
采用新配置的真实W数据预览也通过，下一批8题一致；这仍使用已知h0，不能标作已选出
真实h1或已执行GPU续训。当前没有可提交的正式续训计划；prepare必须先得到完整真实
搜索结果。新入口与源码只在ours/，本轮0新增GPU/API费用、0新模型检查点、0清理。

**下一步只等待此次外发范围确认。** GLM提案调用被自动审批拒绝：现有授权被认为未明确
覆盖这批视觉代码/反馈外发。动作未启动，新增API调用/费用均为0；旧32道Chess外发授权
不扩写成新128道视觉授权。[具体清单](results/visual-proposer-transfer-review-20260910-v1.json)
共8份输入、43,161字节，包含h0、H128反馈与提案规则；C/V/R/T、图像与模型权重不发送。
计划仍为一个真实候选、既有45元累计上限，无自动付费重试。首次出现此审批阻点，目标
保持进行中；没有把审批问题标作科学停止结论，也未改用其他方式执行被拒绝的外发。

证据：[搜索入口计划](data/visual-search-bridge-theta1-20260910-v1/plan.json)、
[真实初始接受](data/visual-search-bridge-theta1-20260910-v1/initial-acceptance.json)、
[四项控制流检查](results/visual-search-bridge-tests-20260910-v1.log)、
[原生视觉数据恢复](results/visual-native-data-resume-20260910-v1.json)、
[新续训配置的CPU输入预览](results/visual-resume-input-preflight-20260910-v2.json)。

## 当前摘要（2026-09-10 17:28，香港时间）

**直接视觉验证已经开展；51组只保留为历史论文设计，不是当前执行任务，也不要求用户增加空间。**
看图117/128、不给图64/128，说明这一小批图表中图像确实提供了答题信息。首次小训练后，
两次同题测量分别为113/128和115/128，配对分别49/64和51/64；初始模型两次均为117/128、
53/64。不能只报告较高的一次，也不能把首次下降的4题全部解释为稳定的训练影响。
当前尚无WHALE交替优化导致视觉退化、或VETO改善结果的科学证据。

17:27核对：224196正在执行训练后模型的搜索题/接受题基准与重复测量；它尚未完成，
也尚未产生新的视觉候选策略或VETO选择结果。当前可用约191GiB，本轮复用现有模型、
不产生新模型检查点。下一步限定于单一图表任务、现有4B模型、单个种子的小规模闭环，
再做原WHALE与VETO的初筛；是否扩展取决于真实信号和当时容量。此初筛不能代替正式
多种子证据。Chess续训、其他域/规模及51组正式扩展均暂缓；不新增清理，不动B组。

最新重复测量证据：[两个固定模型的复测](data/visual-followup-repeatability-20260910-v1/result.json)。
下方16:58及更早记录保留原始时间与证据；其中运行状态以本摘要和后续回执为准。

更新于2026-09-10 16:58，香港时间。**首次真实视觉训练及同题复测已完成：训练前117/128、53/64对；训练后113/128、49/64对。** 单图下降3.125个百分点、配对下降6.25个百分点；4道原先答对的题变错，其余124道答案相同。723个活动张量的完整比较确认参数改变，实际服务加载了更新后的模型。128道题的原始请求像素/提示token/解码，以及归档的处理后像素张量/位置编码/提示逐一相同。重复测量224157于16:57:03启动，对两个固定模型各重做完整128题，检查推理波动；不得挑选较高的一次分数。Chess续训暂缓，51组矩阵仍是历史设计，不是当前队列。

本轮只有8道独立W训练题、每题8次采样，47条成功轨迹、6次原生优化器更新；未搜索harness、未启用VETO，也不是正式方法或三种子实验。这次普通准确率也下降，因此不满足预登记的“普通准确率不降但配对退化”现象门槛，更不能据此归因于交替优化。固定模型重复测量不属于独立复评池R；训练恢复、正式h0/LR校准及搜索接受仍未完成。

论文目前仍处于验证研究问题的阶段：尚未证明交替优化会损害视觉依据，也没有VETO改善性能的证据。提示词校准仍频繁发生未完成工具作答，223996已明确提前停止并保留144张图的部分结果；未获得整个开发集的分数，未选定正式共同h0。为继续推进，当前先用已验证的直接作答条件完成E4训练接线；它不是正式方法比较，之后仍须校准正式共同策略和训练配置。

已完成训练：[冻结计划v4](results/native-visual-rsft-plan-20260910-v4.json)、[完整批次核验](results/native-visual-rsft-batch-audit-224041-v2.json)、[作业终态与成本](data/native-visual-rsft-20260910-v4/allocation-result.json)。原生seed42从2048道W中随机取32道，再由原生DataLoader选8道；学习率沿用1e-7，仅做一批执行验证。2H800/24CPU实际539秒、0.299444GPUh。随后224121于16:44:48完成规范导出和同题复测，1H800/503秒/0.139722GPUh。原生FP32有3,575,378,309个元素改变；导出BF16有53,104,103个元素改变，全部活动张量逐元素满足精确转换，原生恢复文件保持原样。导出文件9.64GiB，16:41现有可用空间约191.11GiB，无额外清理。原生只存model/extra、不存Adam moments；尚未执行实际恢复。

本轮证据：[前后配对结果](data/native-visual-followup-20260910-v1/comparison.json)、[参数变化与导出](data/native-visual-followup-20260910-v1/transition.json)、[128题处理后输入核验](results/native-visual-followup-input-audit-20260910-v1.json)、[复测终态与费用](data/native-visual-followup-20260910-v1/allocation-result.json)。源图表配对bootstrap区间只描述这个固定开发子集，不代表三种子结论或总体保证。

上一次224008生成64次/128token后，在参数更新前因原生reward-worker分支丢失sample ID而失败，0.108889GPUh已计入。新增继承类只把sample ID放入原生输出附加字段，保留原生成、像素、奖励、mask和成功过滤；原生分支及完整像素归档的2项CPU检查通过。本次重新生成新轨迹，未冒充续训或重用上次回复。当前协议已结4.857500GPUh，224157另预留0.5GPUh，正式扩展继续关闭。[重复测量计划](results/visual-followup-repeatability-plan-20260910-v1.json)和[提交回执](results/visual-followup-repeatability-allocation-20260910-v1/submission.json)固定1H800/12CPU/30分钟上限，0新训练/模型检查点/API调用。

当前权威：[视觉优先与现有空间约束](ours/visual_priority_plan_20260910.md)、[机器可读修订](results/veto-visual-priority-amendment-20260910-v1.json)。原[执行协议](ours/veto_execution_protocol_20260910.md)和[51组研究设想](results/veto-research-matrix-20260910-v1.json)保留；执行顺序和当前规模以上述新修订为准，其余科学诚信与预算边界继续有效。**存储授权：用户只授权A组，明确拒绝B项；A组已执行，其他清理未获授权。**

完成证据：[64对开发验证独立重算](results/visual-development-screen-rescore-20260910-v1.json)、[原始汇总](data/visual-development-screen-20260910-v1/result.json)、[作业与成本回执](data/visual-development-screen-20260910-v1/allocation-result.json)。预先按冻结开发集顺序取前64对，三种条件共用同一初始权重、题目、解码及答案解析规则；不给图对照在实际输入中去掉全部图像。总计640次生成、63112输出token，1H800实际987秒/0.274167GPUh，0 API调用、0新模型检查点。新目录文件逻辑大小5.36GiB，实际分配块约0.372GiB；前者超过原4GiB输出估计，差异保留，不把估计写成实测上限。15:38磁盘实查剩约218.72GiB，40GiB余量未触及。正式扩展仍关闭。

Chess最后候选h15作业223966已完成8/32，搜索控制器报告COMPLETED并保留h10；第二阶段续训未运行。旧顺序控制器已停止，不再自动提交Chess训练。当前第一阶段台账已结算4.090278GPUh，待结算预留为0；这不包含该协议之前的项目费用。节省空间方案目前只有A组清理与相同权重的硬链接去重经过实测，尚不能承诺现有空间容纳原完整矩阵。


[本轮实现与数据证据](results/veto-execution-implementation-check-20260910-v1.json)保存源代码快照、27份证据哈希与冻结入口复核。用户没有其他大容量目录；已按明确授权完成[清理清单](ours/storage_review_20260910.md)的A组：19个旧模型权重blob及symlink、1480个pip缓存文件、完全相同Chess权重的硬链接去重，逐文件释放99.65GiB，可用空间从119.46增至219.14GiB。B项588.28GiB MIMIC原始图像保留。[执行回执](results/veto-authorized-a-cleanup-20260910-v1/result.json)记录前后空间、完整权重SHA、40项保留元数据/状态和33项冻结源码复核。清理前已有空间继续近期工程验收；约520.72GiB是完整正式矩阵的保留规划，并非单次训练需求。完整矩阵的空间门槛仍未通过，后续清理不在本次授权范围。

之前逐次更新中的运行状态已归档到[历史快照](ours/history/PROJECT_STATUS_20260910_before_first_joint.md)。[实验日志](EXPERIMENTS.md)保留失败、恢复修订及完整成本来源。

## 当前视觉结果及其边界

| 64对开发图表的诊断条件 | 单图答对 | 两边均答对的配对 | 实际生成次数 |
|---|---:|---:|---:|
| 直接看图 | 117/128（91.41%） | 53/64（82.81%） | 128 |
| 不给图 | 64/128（50.00%） | 0/64 | 128 |
| 允许放大图像 | 6/128（4.69%） | 0/64 | 384 |

不给图时128次均回答A，同一配对两侧输入和回答相同。放大条件的256次裁图全部返回
图像，但102题最后仍为工具调用，20题没有符合共同规则的明确最终标签，只有6题
提交有效标签且均正确。这里的低分主要反映作答未完成；三个条件没有训练或接受模块，
不能将此表标为WHALE/VETO方法对比或官方PlotQA成绩。独立重算保留源图表成组区间，
这些区间只描述一个固定模型的开发子集，不能替代三个训练种子或封存测试。

最初16图工具试验223978严格评分0/16：32次裁图请求全部把归一化坐标当像素，25次没有返回图像，16题均未完成最终作答。这不能用来判断模型不会读图。

随后223979比较直接作答和修正坐标后的工具作答。直接作答16/16、配对8/8；修正后32次裁图全部返回有效像素，但模型在末行答案之前添加解释，旧整段匹配规则仍记0/16。对保存回复应用事后明确末行解析，两种条件均16/16、8/8。原始零分保持原样，新解析不借助真值，未完成工具调用和歧义答案仍拒绝。这是格式诊断，不能算训练收益或VETO效果。

证据：[初轮失败诊断](results/native-visual-first-screen-diagnosis-20260910-v1.json)、[坐标对照结果](data/visual-coordinate-diagnostic-20260910-v1/result.json)、[保存回复的事后解析](results/visual-coordinate-answer-format-replay-20260910-v1.json)。两次作业均COMPLETED，分别0.043889和0.082778GPUh。随后完成的64对开发图表在运行前固定共同解析规则；封存测试集未参与模型评测。

下方保留之前各组件的检查记录；其中带时间的运行状态属于历史快照，当前运行以本页顶部为准。

## Chess工程进度（剩余条件已暂缓）

所有试验从相同的未训练共同BF16权重开始，训练128题、MH优化32题、test64题不相交；[协议](ours/controlled_pilot_protocol.md)和[划分记录](results/chess-pilot-manifest-20260909.json)已冻结。下表中的“完成”指该训练/搜索阶段及证据闭合，不代表最终准确率通过。

| 条件 | seed42 | seed43 | seed44 |
|---|---|---|---|
| weight-only | 两批及最终规范导出完成；保留预检题序偏差 | 预检通过，按新计划暂缓 | 同左，暂缓 |
| harness-only | 5轮×3候选完成，最终h1 | 独立计划通过，按新计划暂缓 | 独立计划通过，按新计划暂缓 |
| WHALE | 第一阶段及导出完成；五轮搜索控制器完成，保留h10；第二阶段续训暂缓 | 第一阶段计划通过，按新计划暂缓 | 第一阶段计划通过，按新计划暂缓 |
| WHALE-FST | 第一阶段计划通过，按新计划暂缓 | 第一阶段计划通过，按新计划暂缓 | 第一阶段计划通过，按新计划暂缓 |

共同模型：`data/models/qwen3.5-4b-common-bf16-v1`，manifest SHA `260337087e4d739db98c2744cf429678cd2f87674e55cad9f22d3cfbe60c3d1a`。723个活动张量/4,539,265,536元素已完整检查；精度转换不计为学习收益。WHALE-FST的提示词搜索约束含明确标注的公开重建部分。

## 已完成且可核验的结果

**Harness-only，seed42。** 原生五轮均保留h1；全部16个候选评测经过32题回复/token/棋盘检查、实际worker权重与seed检查及Slurm终态核验。[完整搜索证明](results/controlled-harness-only-seed42-completion-20260910.json)。

| 候选 | h0 | h1 | h2 | h3 | h4 | h5 | h6 | h7 | h8 | h9 | h10 | h11 | h12 | h13 | h14 | h15 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 解出题数/32 | 1 | 8 | 5 | 0 | 6 | 7 | 8 | 6 | 7 | 7 | 8 | 6 | 6 | 8 | 8 | 4 |

[完整搜索轨迹图](results/controlled-harness-only-seed42-search-trajectory-20260910-v1.png)及对应CSV/PDF已生成：实际选择只在每轮结束后更新，保留全部16个评测。累计515次任务模型调用、3,991,282输出token、9.165556GPUh。512次题目评测来自反复使用同一MH32，不能当作512道独立题或留出泛化证据。三次提案格式中断及显式恢复修订全部保留；未重复付费提案或已审计的模型评测。h1固定回复在旧parser下动作相同，不能把分数变化归因于parser或声称完成了因果消融。

**Weight-only，seed42。** 原生222801完成两批128条新轨迹：3/64和0/64 accepted，共1次SFT更新、22,989个loss token；139次请求、1,039,118输出token、0错误。[训练终态](results/controlled-pilot-222801/result.json)及[完整轨迹审计](results/controlled-pilot-schedule-deviation-audit-222801.json)。原预检遗漏DataLoader初始化推进随机流，预写题序错误；实际原生配置/seed推导顺序、两批轨迹及保存sampler状态一致。偏差保留，没有改题或重采样。

规范导出223620于07:43:24 COMPLETED/exit0，329秒/1RTX4090/0.091389GPUh。全部723张量逐元素满足原生权重的精确BF16转换；相对共同起点，原生FP32有3,853,212,360元素变化，导出BF16有9,033,512元素变化。两个原生checkpoint的模型字节相同，对应第二批无更新；全部原生恢复状态仍保留。参数变化不证明准确率提高。[导出终态](results/controlled-checkpoint-export-223620/result.json)、[完整参数变化](results/controlled-weight-only-seed42-222801-step2-transition.json)。

## Chess阶段历史记录与暂停范围

WHALE seed42第一阶段223622从独立共同theta0和原h0开始，07:46:21–08:23:08 COMPLETED/exit0，2207秒/2H800/1.226111GPUh。[冻结计划](results/controlled-whale-seed42-phase1-plan-20260910-v1.json)、[完整64条轨迹审计](results/controlled-joint-phase1-223622/batch-audit.json)、[训练终态](results/controlled-joint-phase1-223622/result.json)。68次请求全部返回、517,481输出token、0错误；6/64 accepted，来自00BQD和007fJ各3条；45,996个loss token，1次真实SFT更新。模型、调度器、CPU/CUDA/NumPy/Python RNG及正确sampler进度均已保存核验；原生不保存Adam，尚未执行GPU恢复。训练接受率与独立weight-only第一批的差异不能归因于联合搜索：本阶段尚未修改h0。

联合搜索第一轮已完整评测，原生选择器保留h2；[四次评测和选择快照](results/controlled-mh-223898-completion.json)核对全部原生/token/棋盘、worker和Slurm证据。这里反复使用同一MH32，不是128道独立题；完整五轮搜索仍未结束。

| 联合候选 | h0 | h1 | h2 | h3 |
|---|---|---|---|---|
| 解出题数/32 | 1 | 1 | 5 | 3 |
| 任务模型调用 | 32 | 32 | 32 | 35 |

累计本次联合MH评测131次调用、1,032,116输出token、2.513889GPUh。h2/h3分别1099秒/0.610556GPUh与1129秒/0.627222GPUh，均COMPLETED/exit0。第二轮h4作业223905已完成6/32，1125秒/2H800/0.625000GPUh，32次调用/258316输出token，其中30次长度截断；[完整终态](results/controlled-mh-223905-completion.json)通过。h5作业223908也已完成6/32，1098秒/2H800/0.610000GPUh，32次调用/256806输出token、29次长度截断；[完整终态](results/controlled-mh-223908-completion.json)通过。随后h6完成7/32并在第二轮被选中；第三轮h7/h8/h9分别7/32、8/32、8/32，选择h8。控制器1413742已因第四轮300秒提案超时退出；GPU结果均保留。第四轮9个API请求中6个返回Internal Network Failure，无新候选。不同搜索中的同名harness不是同一个候选，未完成联合条件不能作为最终胜负比较。

[h2固定回复诊断](results/controlled-joint-h2-parser-comparison-20260910-v2.json)确认提示词和声明预算未变，31/32次解析动作与h0不同；5个成功轨迹中4个动作不同，3个成功仍包含长度截断。按原生任务轨迹匹配后，32个请求和输入token相同，仅3个输出token序列完全一致，因此不能把总分差全部归因于parser。附加请求比较的初次按日志行序配对错误已明确纠正；原生得分和固定回复解析统计未受影响。

1. 第一阶段及[规范导出223757](results/joint-checkpoint-export-223757/result.json)完成：244秒/1RTX4090/0.067778GPUh，723活动张量逐元素符合原生BF16转换，9,013,513个变化元素保留到导出。两个实际worker的8坐标和seed42通过，其中5坐标区别于共同theta0；这是抽查，不是所有服务参数的完整证明。[实际交接快照](results/controlled-mh-223824-start-snapshot.json)。原生恢复状态保留。
2. 保留正在完成的同一theta1下第五轮最后候选h15及最终选择；原第二批真实续训计划暂缓，优先开展视觉验证。若将来恢复，仍必须从原生checkpoint继续，不能复用独立weight-only的训练前缀或harness-only的搜索结果。
3. 按已批准的执行修订，暂缓其余Chess条件/种子；不创建最终12-trial cohort，不打开test64。保留原协议和冻结记录，不能将未运行项目标为完成。
4. 直接执行一次小规模真实视觉初筛，再依据图像必要性和实测空间制定后续视觉诊断；不以Chess联合闭环完成为前提。第一阶段100GPUh是上限，当前只安排一项视觉评测，尚未启动正式方法比较。

## 视觉实现的早期检查记录（历史）

以下保留真实GPU诊断之前的检查与边界；本轮实际模型结果以上方新完成表格为准，E2–E4视觉闭环仍未完成。

**本轮新增。** [真实视觉服务计划](results/native-visual-service-plan-20260910-v1.json)固定共同4B权重、处理器、greedy解码、8对工程图片、原生Manager/Worker和逐请求像素/token证据。1H800/40分钟上限；计划本身不是模型启动成功证明。14:44新视觉优先控制器已通过完整源/输入预检，实际分配只等待账户GPU作业位置空出，不再等待Chess续训或结果验收。`native_visual_service`对应E1真实评测通路，`native_visual_model_worker`记录实际加载后的8个权重坐标，不能扩写为完整内存权重证明。

生成harness的五个函数现可在独立受限进程执行：Landlock只读白名单、seccomp网络/写入/进程创建限制、CPU/内存/超时边界；仅传函数允许看到的输入。内核直接拒绝读私有文件、写入、truncate、chmod、socket和fork，无限循环也被停止；默认五个回调与进程内结果一致。原生视觉服务计划显式启用隔离模式，原始进程内接口仅保留兼容性。此处是共享执行基础设施，不是新增Method模块。

原生视觉配置/16行真实dataset/processor/Worker构造器以及原生配对和harness合计4项检查通过（`native-visual-service-constructor-tests-20260910-v3.log`）；模型回复及Ray服务仍为夹具。最后基础发现171项测试，71通过/100依赖跳过，10.936秒；不能把跳过项写为通过。

**数据建设。** 官方CLEVR元数据与PlotQA训练标注已下载并固定字节身份。PlotQA完整扫描157070条，明确排除15条非有限数值记录，得到41027个竖向柱图候选源表。`PlotQA-EvidencePairs`将其重渲染为二元数值比较：配对交换两个数值单元、答案改变；不是官方PlotQA分数，也不称单因素最小干预。W2048/H128只用未编辑图；C64/V256/T1024/R1024按语义源表分组隔离，R先使用512对。配方/分组已冻结，6912张图的重渲染及独立像素oracle已全部通过；W2048/H128/C128图/V512图已完成原生Parquet转换，0模型调用。源/配方位于`data/plotqa-evidence-pairs-20260910-v1`；尚未通过图像必要性、文本可读性、近重复或科学现象门槛。CLEVR图像/程序配对与ChartQA原任务接口仍待完成。

[VisualEvidenceDataset](ours/visual_evidence_dataset.py)在ours/中继承原生dataset，修复提示替换丢图及长度过滤漏计图像展开；prompt环境进入缓存身份，图像字节不与答案一同传给模型。它是所有视觉对照共用的输入通道，**不是VETO E1–E3的创新**。

两项原生CPU检查使用现有一对工程图片：提示替换前后均保留120个图像token，不同像素仍进入原生SingleTurn请求和训练DataProto，mRoPE与回复掩码保持。无图控制、长度阈值、缓存身份和提示变更拒绝通过。回复和reward明确为合成夹具；未加载模型权重，没有视觉生成、actor前反向或完整视觉训练/搜索结果。[实现与证据记录](results/visual-native-interface-implementation-check-20260910.json)。

[共享视觉奖励](ours/visual_evidence_reward.py)已接通原生reward加载器、manager、worker和RSFT筛选循环的CPU检查。Method为E4的二元验证 `r_i=v(answer_i,y_i)` 及原生 `B+={tau_i:r_i>0.5}`；不改变损失，所有视觉条件共用。2项检查通过：脚本回复得分[1,0,1,0]时只选第0/2行，像素张量和回复掩码保持；全不合规时跳过更新。模型回复、actor操作和Ray传输是夹具，实际模型调用/优化器步数为0，不是视觉准确率。原生动态导入要求`pkg://`，首次缺少此前缀的失败及修正均保留。[奖励集成记录](results/visual-reward-integration-check-20260910.json)。

[共享多轮工具适配](ours/visual_evidence_tool_loop.py)继承原生ToolAgentLoop和ImageZoomInTool：crop使用模型实际看到的初始像素，最后一段assistant决定二元奖励，工具反馈/图像维持原生零loss mask。独立agent/tool YAML及1项原生CPU测试的6种情形全部通过：正确/错误crop回答、无效框、无图、无最终答案、直接回答；无图不能借metadata取回图片，原生工具错误分不进入任务奖励。10次脚本请求，0实际模型调用/优化器步数。实际actor更新、视觉模型服务及搜索编排仍待接通；下述接口已提供CPU候选派发和评测批次执行；不是VETO新机制。[最终工具集成记录](results/visual-tool-integration-check-20260910-v3.json)。

[可执行视觉harness接口](ours/visual_harness.py)已接到原生dataset和tool loop：观察格式、crop参数、反馈、答案解析及预算内追加请求共五个函数。默认h0在测试中保持现有请求/token/像素/mRoPE；候选代码SHA进入缓存和每行数据，数据/rollout身份不符或源文件变化会拒绝。prompt-only只改两条字面提示，非提示AST必须与参考相同；可变函数默认参数也会被拒绝，避免跨题携带状态。2项原生CPU检查通过，包含实际关键词dataset工厂、Hydra加载、160→128的真实crop、反馈/追加请求、原始与提交答案、有效操作哈希和有/无工具批次合并。模型回复仍是脚本，0实际模型调用/优化器步数；AST检查不等于OS隔离。实际视觉proposer、GPU服务启动/计划和VETO接受位置仍待接通，强视觉h0与正式视觉数据协议尚待建立。[候选执行记录](results/visual-harness-integration-check-20260910-v2.json)。

[原生配对评测入口](ours/visual_native_evaluation.py)已在CPU接通原生Manager分批、Worker生成入口、Hydra视觉agent、crop、DataProto归档与E1配对统计。配对Parquet只把像素/问题放进模型消息；生成前检查完整覆盖及图像/标签/问题与manifest一致。返回按样本身份合并，独立核对parser、reward和mask；缺一侧、不均匀worker批次、重复身份或错误reward均拒绝，后续批次中断不产生整集得分。真实生成次数、返回assistant token、保留loss token与原生turns分开记录，不改变E3排序。最终评测1项原生检查23.076秒通过，另两项harness检查通过；Manager/Worker构造器未执行，Ray RPC和模型回复为夹具，0实际模型/API调用或优化器步数。首次冷启动导入失败保留；实际模型身份/解码、服务启动和VETO接受尚未由此验证。[完整记录](results/visual-evaluation-integration-check-20260910.json)。

视觉依赖安装在独立、显式启用的`data/visual-runtime-overlay-v1`，没有改动冻结的Chess训练环境。共同模型与223620导出模型的视觉/无图、thinking开关共8种输入检查得到完全相同的处理器张量和mRoPE，证明此次资产规范化保持这些输入的语义，不代表视觉性能通过。

VETO仍是待验证假设：E1配对视觉正确率、E2固定入阶段参考的非退化约束、E3先任务分数后原生turns；E4属于原WHALE的成功筛选训练。原F0十二项前置已由用户批准的执行协议替代；seed42联合闭环尚未完成，视觉科学门槛仍未通过。此前16张图的3B视觉工程检查及旧训练权重上的0/32→6/32，均与本轮受控试验分开保留。

## 较早成本、资源和验证快照

截至223908终态，已知项目累计25.849444GPUh（含已归档失败），当前h6尚未计入终态成本。10:48保守API项目记账0.613044CNY，82次已结、0未结，45元预算内可预留44.386956元；不是官方账单或账户余额。原生日志中的美元估价沿用上游假设，不能作为GLM实际支出。

10:48账户GPU配额实时查询：总3700h，已用787.416667h，剩2912.583333h（21.28%）；这是账户总量，不能混同项目成本。同一快照主目录剩120.26GiB，可支撑下一阶段，但不足以保留完整矩阵预估的后续约430GiB；用户允许先提出清理清单，但删除必须单独获得其明确同意；正式矩阵扩展前重新核算空间。未删除或压缩既有checkpoint。

基础检查125通过/99依赖跳过（随后新增的故障情形已由原生测试执行）；视觉dataset原生2项、奖励原生2项、多轮工具原生1项/6情形、候选派发原生2项、配对评测原生1项通过，完整导出与资产等价检查通过；16份既有冻结执行记录中155个实际代码/配置文件重哈希一致（同时纳入joint_source_sha256；此前153为主清单路径字符串，对应149个实际文件），此次未重新哈希大型权重。联合GPU恢复、完整多种子和真实留出验证仍待执行。通知已配置，实际送达未验证。上游固定`fbe125eb7abea7f760c99ab9acc1a6261e708fc6`且工作区干净；新增实现只在ours/，更改仅本地，未push，未修改beyond-entropy。

# 首轮完成后的资源决策（2026-09-16，待确认）

首轮真实视觉闭环已经完成，当前没有GPU作业运行。已结15.9383/100 GPU小时；当前新增目录占用约123.31/400 GiB。三种选择规则均选h3，方法优越性尚未建立。

完整范围按已经完成的任务扣减后，预计总计82.40 GPU小时（含剩余故障重跑额度）。全部模型保留约700.78 GiB，超出400 GiB上限。详细测量见[资源核验](../results/fast-chart-completed-cycle-resource-review-20260916-v1.json)。未减少种子或条件，没有把已执行工作重复收费。

## 决定一：按具体清单回收模型文件

这沿用9月15日已提交、尚未获同意的[存储保留清单](fast_chart_retention_20260915.md)，不涉及旧B组或其他项目。

以下路径相对于`/userhome/cs3/yihangc/Documents/whale-delta/data/fast-chart-20260915-v1/`：

| 当前可供批准的目录 | 占用GiB | 保留方案与删除影响 |
|---|---:|---|
| `lr-calibration-v3/lr-1e-07-seed42/training/checkpoints/global_step_4` | 18.145 | 保留结果、配置、原始回答、图像/梯度/参数审计；无法再直接恢复此未选中的训练检查点。 |
| `lr-calibration-v3/lr-1e-07-seed42/followup/hf-canonical` | 9.651 | 保留该学习率的完整评测；无法直接加载此未选中的模型。 |
| `lr-calibration-v3/lr-1e-05-seed42/training/checkpoints/global_step_4` | 19.295 | 同上，保留证据而不保留未选中的FP32模型。 |
| `lr-calibration-v3/lr-1e-05-seed42/followup/hf-canonical` | 9.651 | 同上，保留该学习率完整评测。 |
| `formal-v1/seed42/veto/training/checkpoints/global_step_8` | 19.160 | 已完成真实续训、完整参数改变与精确BF16导出核验。保留`followup/hf-canonical`及全部逐题、图像、梯度、参数、成本证据。删除后不能逐位恢复该最终FP32训练状态。 |

前四项目录共56.741 GiB；第五项是原先15个条件最终原生检查点清单中现已完成的一项。剩余14个路径仍按原清单：只有对应真实运行结束、完整核验通过、服务权重保留，且无活动作业或后续续训依赖时才逐个回收。未完成或未核验的模型不得删除。

选中的LR=1e-6共同分叉检查点、三个种子的共同分叉模型、初始模型、最终BF16服务模型、数据和全部结果均保留。已列出的路径和引用见[更新后的机器清单](../results/fast-chart-resource-decisions-20260916-v1.json)，前四项的完整文件身份与引用仍在[原机器清单](../results/fast-chart-retention-proposal-20260915-v1.json)。永久删除前还要重查实际文件身份及引用，不能仅凭旧记录执行。

按上述方案顺序运行，实测外推峰值约374.05 GiB。若额外保留当前seed42最终FP32模型，但仍回收将来14个最终检查点，约393.20 GiB，余量较小。这两个数字都依赖逐个回收方案；只删除56.741 GiB校准模型而保留所有将来模型仍不够。预测不是空间保证，每次作业前继续执行400 GiB及文件系统余量检查。

## 决定二：只调换阶段额度，不增加总预算

| 阶段 | 当前上限GPUh | 建议上限GPUh | 尚待工作预测GPUh |
|---|---:|---:|---:|
| 核心对照 | 40 | 35 | 28.35 |
| 最终及外部评测 | 20 | 25 | 23.52 |

准备阶段20、消融15、故障重跑5、GPU总计100、API总计45元和新增存储400 GiB均不变。三个种子、18个逻辑条件、所有实验设置及科学门槛不变。

## 当前执行边界

目前未删除上述文件，也未修改阶段额度。继续正式矩阵需要处理这两项资源限制；完整结果核验和本地写作已经完成相应更新。

需要明确确认的依据是已批准精简计划中的“中间文件若需回收，先提供具体路径及引用清单，另行取得删除授权”，以及不能静默突破阶段额度的要求。此前旧A组授权不涵盖这些9月15日新模型；本次公开GitHub授权也不涵盖删除模型或改预算。若不批准，可保留所有现有模型，交付当前诊断材料，再另行确定缩减后的研究范围；不会把未执行条件写成已完成实验。
